# NPM Configuration for @mentiora/sdk
# This file configures npm for publishing private packages

# Use npm registry (default)
registry=https://registry.npmjs.org/

# For scoped packages, ensure they're treated as private
# @mentiora:registry=https://registry.npmjs.org/
